Index: dist/modules/CdxTextInput.cjs
===================================================================
--- dist/modules/CdxTextInput.cjs
+++ dist/modules/CdxTextInput.cjs
@@ -12,19 +12,19 @@
   v = require('./Icon.js'),
   r = require('./constants.js'),
   F = require('./useModelWrapper.cjs'),
   N = require('./useSplitAttributes.cjs'),
-  j = require('./useFieldData.cjs'),
-  P = require('./_plugin-vue_export-helper.js'),
-  U = r.makeStringTypeValidator(r.TextInputTypes),
+  P = require('./useFieldData.cjs'),
+  U = require('./_plugin-vue_export-helper.js'),
+  j = r.makeStringTypeValidator(r.TextInputTypes),
   z = r.makeStringTypeValidator(r.ValidationStatusTypes),
   R = n.defineComponent({
     name: 'CdxTextInput',
     components: { CdxIcon: v.CdxIcon },
     inheritAttrs: !1,
     props: {
       modelValue: { type: [String, Number], default: '' },
-      inputType: { type: String, default: 'text', validator: U },
+      inputType: { type: String, default: 'text', validator: j },
       status: { type: String, default: 'default', validator: z },
       disabled: { type: Boolean, default: !1 },
       startIcon: { type: [String, Object], default: void 0 },
       endIcon: { type: [String, Object], default: void 0 },
@@ -48,9 +48,9 @@
         {
           computedDisabled: a,
           computedStatus: p,
           computedInputId: d,
-        } = j(n.toRef(t, 'disabled'), n.toRef(t, 'status'), s),
+        } = P(n.toRef(t, 'disabled'), n.toRef(t, 'status'), s),
         i = n.inject(r.FieldDescriptionIdKey, void 0),
         l = F(n.toRef(t, 'modelValue'), o),
         c = n.computed(() => t.clearable && !!l.value && !a.value),
         b = n.computed(() => ({
@@ -116,9 +116,9 @@
         onInvalid: (e, y) => {
           ;(y && e.preventDefault(), o('invalid', e), (m.value = !0))
         },
         shouldPreventDefault: m,
-        cdxIconClear: v.j3,
+        cdxIconClear: v._5,
       }
     },
     methods: {
       focus() {
@@ -234,6 +234,6 @@
       6,
     )
   )
 }
-const W = P._export_sfc(R, [['render', O]])
+const W = U._export_sfc(R, [['render', O]])
 module.exports = W
