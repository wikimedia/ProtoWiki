/** Preset identifiers for app chrome header tool buttons; order follows layout. */
export type AppHeaderTool = 'tabs' | 'notifications'

export const DEFAULT_APP_HEADER_TOOLS: AppHeaderTool[] = ['tabs', 'notifications']
